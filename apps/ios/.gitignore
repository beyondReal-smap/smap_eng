# Xcode
build/
DerivedData/
*.xcuserstate
*.xcuserdatad/
xcuserdata/

# xcodegen 결과물 (재생성 가능)
HaruBook.xcodeproj/
SmapEng.xcodeproj/

# Swift Package Manager
.build/
Package.resolved

# OS
.DS_Store

# Firebase — GoogleService-Info.plist 는 시크릿(API 키, OAuth client ID 포함) 이라 커밋 금지.
# Firebase Console > 프로젝트 설정 > iOS 앱 > GoogleService-Info.plist 다운로드 후
# apps/ios/HaruBook/ 에 배치한다. project.yml 의 `sources: - path: HaruBook` 가 자동 번들링.
HaruBook/GoogleService-Info.plist
GoogleService-Info.plist
